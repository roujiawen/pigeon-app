{
  "name": "pigeon-app-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "check": "pnpm typecheck && pnpm lint && pnpm test"
  }
}
